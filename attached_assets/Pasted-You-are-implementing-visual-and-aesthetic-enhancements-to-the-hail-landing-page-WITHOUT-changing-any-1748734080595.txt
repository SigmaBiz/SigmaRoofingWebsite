You are implementing visual and aesthetic enhancements to the hail landing page WITHOUT changing any functionality, forms, API integrations, or dynamic content. All existing functionality must remain exactly as is - we are ONLY adding visual appeal.

CRITICAL: Do NOT modify any form logic, API calls, state management, or dynamic storm content functionality. These must work exactly as they currently do.

## Visual Enhancement Implementation Plan:

### 1. FULL-PAGE WEATHER BACKGROUND SYSTEM
- Add a new image field to the admin panel called "Hail Landing Page Background"
- Implement a full-viewport background image that spans the entire page behind all content
- Apply a dark gradient overlay (rgba(0,0,0,0.4) to rgba(0,0,0,0.7)) to ensure text readability
- Use CSS: background-attachment: fixed for parallax effect
- Add backdrop-filter: blur(1px) to sections for glassmorphism effect
- Ensure the background image is controlled via admin panel just like other images

### 2. ENHANCE THE STORM REPORT CARD
- Add a pulsing red glow animation to the alert icon (subtle box-shadow animation)
- Apply gradient backgrounds to the storm data boxes (e.g., from red-50 to orange-50)
- Add drop shadows to make the card appear elevated
- Include subtle weather animations: use CSS to create small rotating icons or pulsing effects
- Enhance the warning box with a gradient border animation

### 3. VISUAL BREAK SECTIONS
- Add capability for full-width image sections between major content areas
- Create admin controls for "Section Break Image 1", "Section Break Image 2", etc.
- These images should have parallax scrolling effects
- Apply dark overlay gradients to maintain readability of any overlaid text

### 4. ENHANCED STYLING
- Replace all flat color backgrounds with subtle gradients
- Add box-shadows to all cards and form elements for depth
- Implement hover effects: scale(1.02) transforms and shadow increases
- Add text-shadows to all headlines for better contrast against backgrounds
- Make the CTA button pulse with a gentle animation
- Apply glassmorphism to the contact form: semi-transparent background with backdrop blur

### 5. URGENCY ANIMATIONS
- Make the urgency message box glow with a subtle orange pulse animation
- Add a subtle shake animation to the phone number on hover
- Create a gentle bounce animation for the main CTA button

### 6. TRUST INDICATORS SECTION
- Add a new section after the hero for trust badges
- Include placeholder areas for: NOAA data badge, Insurance logos, BBB rating
- Apply subtle float animations to these badges
- These images should be controllable via admin: "Trust Badge 1", "Trust Badge 2", etc.

### 7. WEATHER DATA VISUALIZATION
- Next to hail size text, add a visual size comparison using CSS circles
- Show coin references (penny, quarter, golf ball) as size indicators
- Add subtle animations to these size indicators

### 8. LOADING STATES
- Enhance the current loading spinner with a storm cloud animation
- Add skeleton loading effects with gradient animations

### 9. CSS ENHANCEMENTS
- Use CSS custom properties for all gradients and shadows for consistency
- Implement smooth transitions (0.3s) on all interactive elements
- Add will-change properties for optimized animations
- Ensure all animations respect prefers-reduced-motion

### 10. MOBILE OPTIMIZATIONS
- Disable parallax on mobile for performance
- Reduce animation complexity on mobile devices
- Ensure background images work well on all screen sizes

IMPLEMENTATION NOTES:
- The dramatic background should create emotional impact without interfering with content
- All text must remain highly readable with proper contrast
- Performance is key - use CSS animations, not JavaScript
- The form functionality and API integrations must remain UNTOUCHED
- Only add visual enhancements that complement the existing structure

The goal is to transform this from a functional landing page into a visually stunning experience that conveys the urgency and drama of storm damage while maintaining trust and professionalism. The page should feel alive with subtle movements and depth while keeping all current functionality intact.